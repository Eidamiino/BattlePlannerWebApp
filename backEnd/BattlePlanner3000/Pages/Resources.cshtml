@model BattlePlanner3000.Pages.Resources
@{
}
<script id="resources" type="x-tmpl-mustache">
  <li>{{name}}</li>
    <ul>
      <li>{{#requirementList}}{{#requirement}}{{name}}{{/requirement}}{{/requirementList}}-{{#requirementList}}{{amount}}{{/requirementList}}</li>
    </ul>
</script>
<h1>Resources</h1>
<ul id="resourceList">
  <h4>Total items: @Model.TotalItems</h4>
</ul>
<div class="mb-3">
    <div class="form-label">Name:</div> 
    @* <input asp-for="NewResourceName" value="@Model.NewResourceName" class="form-control"/> *@
    <input type="text" class="form-control" id="ResourceName"/>
  </div>
  <div class="mb-3">
    <div class="form-label">Requirement Name:</div>
    <select asp-for="RequirementName" value="@Model.RequirementName" id="choices" class="form-control">
    </select>
    @*<input asp-for="RequirementName" value="@Model.RequirementName" class="form-control"/>*@
  </div>
  <div class="mb-3">
    <div class="form-label">Requirement Amount:</div>
    @* <input asp-for="RequirementCapacity" value="@Model.RequirementCapacity" class="form-control"/> *@
    <input type="number" class="form-control" id="RequirementCapacity"/>
  </div>
<div class="mb-3">
  <button onclick="onBtnCreateResourceAsync()" class="btn btn-primary">Submit</button>
</div>

@* @if (Model.RequirementCapacity>=0)       *@
@* {       *@
@*   <div class="alert alert-danger" role="alert">     *@
@*     The requirement amount is not valid     *@
@*   </div>     *@
@* }    *@